<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav"
    app:startDestination="@id/wallets">
    <fragment
        android:id="@+id/wallets"
        android:name="com.dds.loftcoins.ux.wallets.WalletsFragment"
        android:label="@string/fragment_wallets"
        tools:layout="@layout/fragment_wallets"/>
    <fragment
        android:id="@+id/rates"
        android:name="com.dds.loftcoins.ux.rates.RatesFragment"
        android:label="@string/fragment_rates"
        tools:layout="@layout/fragment_rates"/>
    <fragment
        android:id="@+id/converter"
        android:name="com.dds.loftcoins.ux.converter.ConverterFragment"
        android:label="@string/fragment_converter"
        tools:layout="@layout/fragment_converter"/>
    <dialog
        android:id="@+id/currency_dialog"
        android:name="com.dds.loftcoins.ux.currency.CurrencyDialog"
        android:label="CurrencyDialog"
        tools:layout="@layout/dialog_currency" />
</navigation>